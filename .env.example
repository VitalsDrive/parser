SUPABASE_URL=https://odwctmlawibhaclptsew.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
PARSER_PORT=5050
LOG_LEVEL=info
SUPABASE_QUEUE_MAX_SIZE=1000
HEALTH_PORT=8080

# OBD devices are auto-provisioned via IMEI lookup in the devices table.
# No fleet_code or DEFAULT_VEHICLE_ID needed - the parser looks up
# vehicle_id by IMEI from the devices table and writes telemetry directly.
